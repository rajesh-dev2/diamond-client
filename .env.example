# VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_BASE_URL=http://15.252.138.172:5000/api
# Socket.IO defaults to the same host as VITE_API_BASE_URL (minus the /api path).
# Only set this if the socket server lives on a different host.
# VITE_SOCKET_URL=http://localhost:5000
